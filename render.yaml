services
  - type web
    name ancient-dna-analysis-api
    env python
    plan free
    buildCommand pip install -r requirements.txt
    startCommand uvicorn mainapp --host 0.0.0.0 --port $PORT
    envVars
      - key GOOGLE_API_KEY
        sync true
